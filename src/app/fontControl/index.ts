import { geistMono } from "./_geistMono";
import { geistSans } from "./_geistSans";
import { gotham } from "./_gotham";
import { roboto } from "./_roboto";

export const fontNames = `${geistSans.variable} ${geistMono.variable} ${gotham.variable} ${roboto.variable} antialiased`;
